import React from 'react'
import Calendar from 'react-calendar';

export default function Date() {
  return (
    <span>
        <h4>Select a Date</h4>
        <Calendar></Calendar>
    </span>
  )
}
